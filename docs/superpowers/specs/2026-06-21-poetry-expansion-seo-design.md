# Spec: 诗词朝代扩展 + 全站 SEO (Poetry Era Expansion + Site-wide SEO)

## Goal

Two related changes driven by user requests:

1. **诗词朝代扩展**: Expand the existing `/poetry` module beyond 唐诗/宋词/元曲 to cover pre-Tang 诗体 (汉乐府, 南北朝) and 骈文 (parallel prose). Add a `form` (体裁) filter axis perpendicular to the existing category axis so users can browse "all 五绝 / 七律 / 古风 / 乐府" poems across all eras.

2. **全站 SEO 深化**: Add a coherent SEO layer across the site — dynamic sitemap, dynamic robots.txt, unique metadata + canonical URLs on every content detail page, and JSON-LD structured data (CreativeWork / Book / DefinedTerm / BreadcrumbList / Organization / WebSite with SearchAction) so search engines can produce rich results.

## Background

- `poems` table currently has ~190K rows across 唐诗 / 宋词 / 元曲. `category` is a free-form VARCHAR (no enum). Existing schema: `id, title, author, dynasty, content (JSON), paragraphs, pinyin, rhythmic, type, ...` — verified via `lib/api-poetry.ts` and `lib/poetry-types.ts`.
- `/poetry` route shows category tabs (唐诗/宋词/元曲) with search/pagination. No form filter exists.
- `/ancient` module (`docs/superpowers/specs/2026-06-20-ancient-classics-design.md`) reads from `classics` table (chunks JSON for long works). 190 books already loaded (375 in production after prod push on 2026-06-21).
- `chinese-poetry/chinese-poetry` GitHub repo has `/全汉诗/`, `/南北朝/`, `/古体诗/`, `/近体诗/` JSONs. Already the data source for 唐诗/宋词/元曲 in `scripts/build-poems.ts`.
- `guwendao.net` scraping is proven (`scripts/build-classics-guwendao.ts`, 75 books, 3021 chapters). Pattern: `https://www.guwendao.net/guwen/bookv_<bookId>.aspx` for chapter content, `https://www.guwendao.net/guwen/book_<bookId>.aspx` for index. Parses `<div class="contson">...</div>` with `<p>` and `<br/>` tags. OpenCC t2s + pinyin-pro per char.
- Next.js 15 already has `MetadataRoute.Sitemap` and `MetadataRoute.Robots` types in `app/sitemap.ts` and `app/robots.ts`. Existing `app/layout.tsx` has minimal global metadata; per-page metadata is partial.
- No JSON-LD currently injected on any page.
- `classics` table is in utf8mb4 with VARCHAR `category` (not enum); new categories like 'pianwen' are free-form.

User decisions confirmed in brainstorm (2026-06-21):
1. Scope = BOTH new dynasty poetry (汉乐府/南北朝/骈文) AND form filter on existing /poetry.
2. New data sources = chinese-poetry (汉乐府/南北朝) + guwendao.net (骈文) — mixed approach.
3. Form annotation = structural inference (五言/七言/绝句/律诗) + source `type` field merge (乐府/古风). 词曲 use 词牌名/套数 as form.
4. Form storage = denormalized single column `form VARCHAR(32)` on `poems` table, indexed. NOT a separate many-to-many table.
5. Reprocessing = one-shot script over all 190K rows. ~5–10 min.
6. UI = form chips multi-select on /poetry page (below category tabs), URL-synced `?form=...`. 骈文 stays in /ancient, not /poetry.
7. New dynasty poem storage = 诗 → `poems` table (category='汉乐府'/'南北朝'); 骈文 → `classics` table (category='pianwen').
8. SEO = 全面深化 across content detail pages; 暂不做百度站长平台集成; 不做 og:image 静态图（去掉了）。
9. SEO routes prioritized = `/poetry/[id]`, `/ancient/[slug]`, `/chars/[char]` get unique metadata + JSON-LD + canonical. Other routes get `export const metadata` static (hardcoded) — only detail pages use `generateMetadata` (dynamic per-row).
10. SEO sitemap = dynamic, split into multiple files at `/sitemap/poetry.xml`, `/sitemap/ancient.xml`, `/sitemap/chars.xml`, referenced from a `/sitemap.xml` index generated by Next.js `app/sitemap.ts` convention. Sub-sitemap routes live in `app/sitemap/poetry.xml/route.ts` etc. (Next.js 15 supports dot-in-folder for route paths).
11. Rollout = all 4 steps run on `piyin_dev` first, smoke tested in browser, then re-run on prod `piyin`.

## Design

### Part 1: Schema changes (poems)

Add one column + one index to `poems` table:

```sql
ALTER TABLE poems
  ADD COLUMN form VARCHAR(32) DEFAULT NULL AFTER content,
  ADD INDEX idx_form (form);
```

- `VARCHAR(32)` accommodates `五言古风` / `水调歌头` / `小令` / `套数`.
- DEFAULT NULL = backward compatible; existing rows unaffected until backfill.
- Single-column index. With 8-cardinality on 190K rows, MySQL uses it for `WHERE form = ?` efficiently.
- DDL is in-place + metadata-only in MySQL 5.7 (no table copy, near-instant on dev/prod).
- No change to `classics` table. 骈文 uses existing schema with `category='pianwen'`.

### Part 2: Form inference algorithm

Pure function in `lib/poetry/infer-form.ts`:

```ts
type FormResult = {
  primary: string;       // '五绝' | '七绝' | '五律' | '七律' | '五言古风' | '七言古风' | '杂言古风' | '乐府' | '古风' | null
  source: 'inferred' | 'source-tag' | 'passthrough';
  confidence: number;    // 0..1
};

function inferFormFromParagraphs(paragraphs: string[]): FormResult
function resolveFormFromSource(type: string | null, rhythmic: string | null, category: string): FormResult
function mergeForm(structInferred: FormResult, sourceTag: FormResult): FormResult
```

**Structural inference rules** (paragraphs = lines of poem):
1. Filter out empty/whitespace-only lines.
2. Compute `lineLengths` = each line's character count (Chinese chars only, no punctuation).
3. Compute `lengthMode` = single value if all `lineLengths` equal, else `mixed`.
   - All 5 → `五言`
   - All 7 → `七言`
   - Mixed → `杂言`
4. Compute `lineCount` = non-empty line count.
   - 4 → `绝句`
   - 8 → `律诗`
   - Other → `古风` (with length prefix)
5. Combine: `五言` + `4 lines` → `五绝`; `七言` + `4 lines` → `七绝`; `五言` + `8 lines` → `五律`; `七言` + `8 lines` → `七律`; `五言` + `>8 lines` → `五言古风`; etc. `杂言` always → `杂言古风`.
6. Confidence = 1.0 for canonical (4/8 lines, uniform 5/7); 0.7 for 6 lines (古风 candidate); 0.5 for irregular.

**Source tag rules**:
- `category='song'` + non-null `rhythmic` → passthrough `form=rhythmic` (e.g., `水调歌头`), source='passthrough'
- `category='yuan'` + `type='套数'` → form='套数'; `type='小令'` → form='小令'
- Non-null `type` from chinese-poetry (e.g., `五言古诗`, `七言绝句`, `乐府`, `古风`, `歌行`) → use as primary form, source='source-tag', confidence=1.0
- Otherwise fall through to structural inference

**Merge rule**:
- If source-tag present and structural agrees → use source-tag, confidence=1.0
- If source-tag present and structural disagrees → use source-tag, confidence=0.8
- If source-tag absent → use structural, confidence=structural.confidence

### Part 3: Backfill script `scripts/build-form-tags.ts`

```ts
import { inferFormFromParagraphs, resolveFormFromSource, mergeForm } from '../lib/poetry/infer-form';

// Streams poems in batches of 1000, computes form, UPDATE.
async function backfillForm({ batchSize = 1000, dryRun = false, whereFormNull = true } = {}) { ... }
```

- Reads `SELECT id, category, paragraphs, type, rhythmic FROM poems WHERE (form IS NULL OR ?)` in batches.
- For each row: build `structInferred` from `paragraphs`; build `sourceTag` from `category/type/rhythmic`; `merged = mergeForm(struct, source)`.
- If `merged.primary !== null`: queue `UPDATE poems SET form = ? WHERE id = ?`.
- Logs progress every batch: `[batch 42/191] 42000/190000 done, 39500 form-set, 2500 NULL`.
- `--dry-run` flag: compute and print stats, no UPDATE.
- Idempotent: `whereFormNull=true` (default) limits to unset rows.
- Time estimate: 190K rows × ~1ms compute = 3 min compute + 3 min DB writes = ~6 min total. Within 5–10 min target.

### Part 4: New dynasty poem ingest

**汉乐府 + 南北朝** — extend existing `scripts/build-poems.ts` or new `scripts/build-poems-extra.ts`:

Decision: NEW script `scripts/build-poems-extra.ts` to avoid touching the well-tested `build-poems.ts` path. Both write to `poems` table and `data/poems/<slug>.json` (new source-of-truth dir, matches `data/classics/<slug>.json` pattern).

```ts
const EXTRA_POEM_FILES: ClassicFile[] = [
  // 诗经 already in /classics (chinese-poetry upstream has /诗经/shijing.json)
  // 楚辞 already in /classics
  { path: '/全汉诗/乐府诗.json', slug: 'yuefu', title: '汉乐府', category: '汉乐府', author: null, era: '汉' },
  { path: '/南北朝/南朝诗.json', slug: 'nanbeichao-south', title: '南北朝·南朝诗', category: '南北朝', author: null, era: '南朝' },
  { path: '/南北朝/北朝诗.json', slug: 'nanbeichao-north', title: '南北朝·北朝诗', category: '南北朝', author: null, era: '北朝' },
];
```

- Fetch JSON from `https://raw.githubusercontent.com/chinese-poetry/chinese-poetry/master<path>`.
- **Pre-flight check**: HEAD request each path, fail fast if 404 (raw.githubusercontent.com network was blocked on 2026-06-20; re-verify before running).
- Normalize into `chunks: { id, label, content[], pinyin[] }[]` (one chunk per poem).
- Compute form via `mergeForm` (using `type` from upstream + structural inference).
- UPSERT into `poems` table (slug becomes `id` or new `slug` column? — see Part 4a).
- Write `data/poems/<slug>.json` for source-of-truth.

**4a. poems table 改动**:
- 现有 `poems` 表无 `slug` 列。每行是单首诗，用 `id` 主键。
- 新朝代诗用 auto-increment `id` 续接（与现有 唐诗 同表）。
- 唯一识别靠 `(title, author, content_hash)` 组合去重。Import 时计算 `content_hash = md5(JSON.stringify(paragraphs))`，先 `SELECT` 查重再 INSERT。
- 详见 Part 4b `build-poems-extra.ts` 的去重逻辑。

**4b. `scripts/build-poems-extra.ts` 流程**:
```ts
async function buildExtraPoems() {
  for (const file of EXTRA_POEM_FILES) {
    const raw = await fetchFile(file.path);
    const seeds = normalizePoemJson(raw);  // → Array<{title, author, paragraphs, type?}>
    for (const seed of seeds) {
      const content_hash = md5(JSON.stringify(seed.paragraphs));
      const existing = await pool.execute('SELECT id FROM poems WHERE title = ? AND content_hash = ?', [seed.title, content_hash]);
      if (existing.length > 0) continue;  // 幂等：跳过
      const form = mergeForm(inferFormFromParagraphs(seed.paragraphs), resolveFormFromSource(seed.type, null, file.category));
      const contentJson = JSON.stringify(seed.paragraphs);
      const pinyinJson = JSON.stringify(seed.paragraphs.map(linePinyin));
      await pool.execute('INSERT INTO poems (title, author, dynasty, category, content, paragraphs, pinyin, type, form) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)', [...]);
    }
    // 同时写 data/poems/<slug>.json
    writeSourceOfTruth(file, seeds);
  }
}
```

**骈文** — new `scripts/build-pianwen.ts`:
- Reuses `scripts/build-classics-guwendao.ts` scraping primitives (`fetchChapterList`, `scrapeChapterContent` from `lib/guwendao-scraper.ts` — extract if not already).
- Book list hardcoded: 5–10 骈文 books (候选: 文心雕龙, 昭明文选, 骈体文钞, 六朝文絜, 唐宋八大家文钞部分 — 确切清单见 Open Questions Q2, dev 阶段 fetch 验路径后定).
- Each book's `category='pianwen'`, `author`, `era` set per book.
- Output: `data/classics/<slug>.json` (chunks JSON) + UPSERT into `classics` table.
- Volume split (> 2MB) follows existing `xingshi-hengyan-1/2` pattern.

### Part 5: Form filter UI

**新文件** `components/poetry/FormFilterBar.tsx` (client component):

```tsx
'use client';

interface FormFilterBarProps {
  category: string;          // 'tang' | 'song' | 'yuan' | '汉乐府' | '南北朝' | 'all'
  selectedForms: string[];   // multi-select
  onChange: (forms: string[]) => void;
}

const SHI_FORMS = ['五言', '七言', '五绝', '七绝', '五律', '七律', '古风', '乐府'];
// 词曲 forms 由 server 提供（top 30 词牌名 + 套数/小令）

export function FormFilterBar({ category, selectedForms, onChange }: FormFilterBarProps) { ... }
```

**Server-side form list** in `lib/api-poetry.ts`:
```ts
async function getAvailableForms(category: string): Promise<string[]> {
  // 诗类（唐诗/汉乐府/南北朝）用固定 8 个 form chips
  if (['tang', '汉乐府', '南北朝'].includes(category)) {
    return SHI_FORMS;  // ['五言', '七言', '五绝', '七绝', '五律', '七律', '古风', '乐府']
  }
  // 词用 top 30 词牌名（从 DB 动态取，不硬编码）
  if (category === 'song') {
    return getTopCipaiNames(30);  // SELECT rhythmic, COUNT(*) AS n FROM poems WHERE category='song' AND rhythmic IS NOT NULL GROUP BY rhythmic ORDER BY n DESC LIMIT 30
  }
  // 元曲用固定 2 个
  if (category === 'yuan') {
    return ['小令', '套数'];
  }
  return [];
}
```

**集成点** `app/poetry/page.tsx` (RSC):
- 读 `searchParams.form` (逗号分隔字符串, e.g. `?form=七绝,七律` → 数组 `['七绝', '七律']`).
- 传给 `getPoems({ category, forms })`.
- 列表 query: `WHERE category = ? AND form IN (?)` (or `AND (form IN (?) OR ?)` for "all" category).
- 同步 chips 选中状态到 URL via `router.replace('/poetry?form=...')`.

**视觉** (per Plan E/G 字·韵 style):
- Chips: 圆角 999px, 14px 字号, h-8 padding-y, hover 显示该 form 诗数
- 选中态: 主色 #5A4530 背景 + 白字；未选: #F5EFE6 背景 + #5A4530 字
- 多选: 累加（OR），不是互斥

### Part 6: SEO architecture

**新文件**:
```
lib/seo/
  metadata.ts          # generatePoemMetadata, generateClassicMetadata, generateCharMetadata
  jsonld.ts            # buildCreativeWork, buildBook, buildDefinedTerm, buildBreadcrumbList, buildOrganization, buildWebSite
  canonical.ts         # buildCanonicalUrl(path)

app/
  sitemap.ts           # MetadataRoute.Sitemap
  robots.ts            # MetadataRoute.Robots
  poetry/[id]/page.tsx # 加 generateMetadata + JSON-LD 注入
  ancient/[slug]/page.tsx # 加 generateMetadata + JSON-LD 注入
  chars/[char]/page.tsx # 加 generateMetadata + JSON-LD 注入
  page.tsx             # 首页加 Organization + WebSite JSON-LD
  layout.tsx           # 全局 metadata 升级
```

**`app/sitemap.ts`** — 动态分片:
```ts
import type { MetadataRoute } from 'next';

const SITE = 'https://pinyin.example.com';

export const revalidate = 3600; // 1 hour

export default async function sitemap(): Promise<MetadataRoute.Sitemap> {
  return [
    { url: `${SITE}/`, lastModified: new Date(), priority: 1.0, changeFrequency: 'daily' },
    { url: `${SITE}/poetry`, priority: 0.9, changeFrequency: 'daily' },
    { url: `${SITE}/ancient`, priority: 0.9, changeFrequency: 'weekly' },
    { url: `${SITE}/chars`, priority: 0.9, changeFrequency: 'weekly' },
    // Sitemap index references 分片
    { url: `${SITE}/sitemap/poetry.xml`, lastModified: new Date() },
    { url: `${SITE}/sitemap/ancient.xml`, lastModified: new Date() },
    { url: `${SITE}/sitemap/chars.xml`, lastModified: new Date() },
  ];
}
```

**`app/sitemap/poetry.xml/route.ts`** — 单独 sitemap 分片:
```ts
export const revalidate = 3600;

export async function GET() {
  const poems = await pool.query('SELECT id, updated_at FROM poems ORDER BY id');
  const urls = poems.map(p => `<url><loc>${SITE}/poetry/${p.id}</loc><lastmod>${p.updated_at}</lastmod><priority>0.7</priority></url>`).join('');
  return new Response(`<?xml version="1.0"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls}</urlset>`, { headers: { 'Content-Type': 'application/xml' } });
}
```

- 19万行 → 整 sitemap ~10MB 字符串（协议允许多文件，单文件限制 50MB / 50000 URL）
- 用单个 route 而不是分页，避免 sitemap index 复杂
- `revalidate=3600` = Next.js 缓存 1 小时，爬虫高频访问不击穿 DB

**`app/robots.ts`**:
```ts
export default function robots(): MetadataRoute.Robots {
  return {
    rules: [{ userAgent: '*', allow: '/', disallow: ['/admin', '/api', '/account'] }],
    sitemap: 'https://pinyin.example.com/sitemap.xml',
  };
}
```

**`lib/seo/metadata.ts`** — 详情页 metadata 生成:
```ts
export async function generatePoemMetadata(id: number): Promise<Metadata> {
  const poem = await getPoemById(id);
  if (!poem) return { title: '未找到 | 字·韵' };
  const desc = `${poem.author}《${poem.title}》${poem.paragraphs[0]?.slice(0, 30)}... 拼音注音 | 字·韵`;
  return {
    title: `${poem.title} - ${poem.author} (${dynastyLabel(poem.dynasty)}) | 字·韵`,
    description: desc,
    alternates: { canonical: `https://pinyin.example.com/poetry/${id}` },
    openGraph: { title: poem.title, description: desc, type: 'article' },
    twitter: { card: 'summary', title: poem.title, description: desc },
  };
}
```

- 类似 `generateClassicMetadata(slug)` 和 `generateCharMetadata(char)`
- 每个 detail page 调对应 generateMetadata
- 用 Next.js 15 的 `generateMetadata` async 函数，与 `params` 一起传入

**`lib/seo/jsonld.ts`** — 结构化数据生成器:
```ts
export function buildCreativeWork(poem: Poem) {
  return {
    '@context': 'https://schema.org',
    '@type': 'CreativeWork',
    name: poem.title,
    author: { '@type': 'Person', name: poem.author },
    inLanguage: 'zh-CN',
    isPartOf: { '@type': 'Book', name: dynastyLabel(poem.dynasty) },
    text: poem.paragraphs.join('\n'),
  };
}

export function buildBook(classic: Classic) {
  return {
    '@context': 'https://schema.org',
    '@type': 'Book',
    name: classic.title,
    author: { '@type': 'Person', name: classic.author || '佚名' },
    inLanguage: 'zh-CN',
    datePublished: classic.era,
  };
}

export function buildDefinedTerm(char: Char) {
  return {
    '@context': 'https://schema.org',
    '@type': 'DefinedTerm',
    name: char.char,
    description: char.meaning_zh,
    inLanguage: 'zh-CN',
  };
}

export function buildBreadcrumbList(items: { name: string; url: string }[]) { ... }
export function buildOrganization(site: SiteConfig) { ... }
export function buildWebSite(site: SiteConfig) {
  return {
    '@context': 'https://schema.org',
    '@type': 'WebSite',
    name: '字·韵',
    url: 'https://pinyin.example.com',
    potentialAction: {
      '@type': 'SearchAction',
      target: { '@type': 'EntryPoint', urlTemplate: 'https://pinyin.example.com/search?q={search_term_string}' },
      'query-input': 'required name=search_term_string',
    },
  };
}
```

**JSON-LD 注入**:
- 详情页: 在 page.tsx 末尾 `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(buildCreativeWork(poem)) }} />`
- 首页: 同上，buildOrganization + buildWebSite
- BreadcrumbList: 详情页末尾追加

### Part 7: Rollout (dev first, then prod)

**Step 1: Schema migration on piyin_dev**
```bash
DATABASE_URL="mysql://root:Admin909217@127.0.0.1:3306/piyin_dev" \
  pnpm tsx scripts/migrate-add-form-column.ts
```

**Step 2: Form backfill on piyin_dev**
```bash
DATABASE_URL="...piyin_dev" pnpm tsx scripts/build-form-tags.ts --dry-run   # 看分布
DATABASE_URL="...piyin_dev" pnpm tsx scripts/build-form-tags.ts             # 实际写
```

**Step 3: New content on piyin_dev**
```bash
DATABASE_URL="...piyin_dev" pnpm tsx scripts/build-poems-extra.ts
DATABASE_URL="...piyin_dev" pnpm tsx scripts/build-pianwen.ts
```

**Step 4: UI dev (本地烟测)**
- `pnpm dev`，浏览器访问 /poetry
- 验证 form chips 工作
- 验证新朝代诗可读

**Step 5: SEO dev**
- 写完所有 lib/seo + app/sitemap.ts + app/robots.ts + 详情页 generateMetadata
- `pnpm dev`，浏览器 View Source 看 head + JSON-LD
- 抓 /sitemap.xml, /sitemap/poetry.xml, /robots.txt

**Step 6: 同步到 prod**
```bash
DATABASE_URL="mysql://piyin:Admin909217@139.5.108.245:3306/piyin" pnpm tsx scripts/migrate-add-form-column.ts
DATABASE_URL="...piyin" pnpm tsx scripts/build-form-tags.ts
DATABASE_URL="...piyin" pnpm tsx scripts/build-poems-extra.ts
DATABASE_URL="...piyin" pnpm tsx scripts/build-pianwen.ts
git push origin main   # 触发 prod 部署
```

**回滚预案**:
- Step 1: `ALTER TABLE poems DROP COLUMN form, DROP INDEX idx_form` (DDL instant in MySQL 5.7)
- Step 2: `UPDATE poems SET form = NULL` (full clear); 重跑 build-form-tags 推断结果应一致
- Step 3: `DELETE FROM poems WHERE category IN ('汉乐府', '南北朝') AND id > <last_existing_id>`
- Step 4-5: revert commit
- Step 6: revert commit on prod, re-run DB steps

### File Structure Summary

```
NEW FILES:
  lib/poetry/infer-form.ts                  # form 推断纯函数
  lib/seo/metadata.ts                       # 详情页 metadata 生成
  lib/seo/jsonld.ts                         # JSON-LD 构建器
  lib/seo/canonical.ts                      # canonical URL 工具
  components/poetry/FormFilterBar.tsx       # form chips UI
  app/sitemap.ts                            # Next.js dynamic sitemap
  app/sitemap/poetry.xml/route.ts           # 诗 sitemap 分片
  app/sitemap/ancient.xml/route.ts          # 古籍 sitemap 分片
  app/sitemap/chars.xml/route.ts            # 字 sitemap 分片
  app/robots.ts                             # dynamic robots.txt
  scripts/migrate-add-form-column.ts        # ALTER TABLE
  scripts/build-form-tags.ts                # 一次性 form 回填
  scripts/build-poems-extra.ts              # 汉乐府 + 南北朝
  scripts/build-pianwen.ts                  # 骈文抓取
  data/poems/<slug>.json                    # 新朝代诗 source of truth (gitignored 大文件按需)
  tests/unit/lib/poetry/infer-form.test.ts
  tests/unit/lib/seo/metadata.test.ts
  tests/unit/lib/seo/jsonld.test.ts
  tests/integration/api/poetry-form-filter.test.ts
  tests/integration/app/sitemap.test.ts
  tests/integration/app/robots.test.ts

MODIFIED FILES:
  app/layout.tsx                            # 全局 metadata 升级
  app/page.tsx                              # 首页加 Organization + WebSite JSON-LD
  app/poetry/page.tsx                       # 接 searchParams.form + 渲染 FormFilterBar
  app/poetry/[id]/page.tsx                  # generateMetadata + JSON-LD
  app/ancient/[slug]/page.tsx               # generateMetadata + JSON-LD
  app/chars/[char]/page.tsx                 # generateMetadata + JSON-LD
  lib/api-poetry.ts                         # getPoems 支持 forms[] 参数; getAvailableForms 新函数
  lib/db.ts                                 # 可能需要分页查询 helper
  package.json                              # scripts 增量

NOT TOUCHED:
  app/admin/* (SEO 不管后台)
  app/sutra/[id]/page.tsx (本期范围)
  app/worksheet/* (本期范围)
  app/api/* (sitemap 用 RSC route，不走 API)
  classics table schema (骈文用现有 schema + category='pianwen')
  poems table 其他列
```

### Testing Strategy

**Unit (vitest, 新增)**:
- `tests/unit/lib/poetry/infer-form.test.ts`: ≥20 fixture cases covering 五绝/七绝/五律/七律/古风/杂言/乐府/词牌名/套数/小令, 各种 edge case (空行/标点/混合行数)
- `tests/unit/lib/seo/metadata.test.ts`: snapshot tests for generatePoemMetadata, generateClassicMetadata, generateCharMetadata
- `tests/unit/lib/seo/jsonld.test.ts`: snapshot tests for each schema type

**Integration (vitest, 新增)**:
- `tests/integration/scripts/build-form-tags.test.ts`: mock pool, verify SQL + batch logic
- `tests/integration/api/poetry-form-filter.test.ts`: form filter query returns correct rows
- `tests/integration/app/sitemap.test.ts`: fetch /sitemap.xml, verify contains 100+ poem URLs + 3 分片 references
- `tests/integration/app/sitemap-poetry.test.ts`: fetch /sitemap/poetry.xml, verify all 190K poem URLs present
- `tests/integration/app/robots.test.ts`: fetch /robots.txt, verify allow/disallow/sitemap

**Regression**:
- `pnpm tsc --noEmit` clean
- `pnpm build` exit 0 (per memory rule `feedback-per-task-build-check`)
- `pnpm test` ≥ 575/578 (G6 baseline) — 不掉到 575 以下
- 现有 /poetry 路由在无 form filter 时仍能跑 (form=NULL or default=all)

**Smoke (人验, 8-step 沿用 G6 模板)**:
1. /poetry → category=唐诗 → form chips 显示，hover 数字 = DB 实际数
2. form chip 选 七绝 → 列表只剩 七绝 诗，分页正确
3. form chip 多选（七绝 + 七律）→ 列表是两类的并集
4. URL 复制 `?form=七绝,七律` 刷新 → 状态保留
5. category 切到 宋词 → form chips 自动切到词牌名（前 30 + 搜索框）
6. category 切到 元曲 → form chips 切到 套数/小令
7. /poetry/<id>（某首 汉乐府）→ 详情正常显示，pinyin 完整
8. /ancient 列表 → 骈文集出现，点击进入阅读器正常

**SEO smoke (额外 5-step)**:
9. 浏览器 DevTools View Source → head 有 unique title/description/canonical
10. 抓 /sitemap.xml → 200 OK，引用 3 个分片
11. 抓 /sitemap/poetry.xml → 200 OK，包含所有诗 URL
12. 抓 /robots.txt → 含 Sitemap 指向
13. 浏览器 DevTools → 详情页有 JSON-LD `<script type="application/ld+json">` 含 CreativeWork/Book/DefinedTerm

### Risks & Mitigations

| Risk | Mitigation |
|---|---|
| raw.githubusercontent.com 网络阻塞（已发生 2026-06-20） | build-poems-extra.ts 先 HEAD 验路径；fail 立即 abort + clear error |
| Form 推断误判（如 七言古风 被判七绝） | minimum 置信度 0.7；6 行以上才标古风；fallback 到源 type |
| 词牌名 form 枚举爆炸（数百个） | 只显示 top 30 + 搜索框；不强制全显示 |
| 19万行 sitemap 单文件 10MB | 协议允许 50MB；如超限可加 lastmod-based 分片 |
| JSON-LD 字段错误 | 严格单测 snapshot；CI 跑 Google Rich Results 校验（可选） |
| 骈文 长篇抓取超时 | 沿用 G6 split 卷模式 |
| ALTER TABLE 在大表锁表 | MySQL 5.7 ADD COLUMN = in-place + instant；先在 dev 验证 |
| SEO 改动破坏 RSC 缓存 | 详情页 generateMetadata 改 cache 策略；用 revalidate 控制 |

### Out of Scope (YAGNI)

- og:image 动态生成（去掉了，per user decision）
- 百度站长平台 集成（暂不做，per user decision）
- 视频/音频 SEO（无相关 content）
- i18n hreflang（单语 zh-CN）
- HSTS / CSP / 安全 headers（与 SEO 无关，独立 plan）
- sitemap 实时推送（依赖外部服务，先静态）
- 多 form tag 关联表（YAGNI，单 form 列够用）
- Schema.org DefinedTermSet（一个字独立 page，不需要 set）
- 自动 sitemap 分片按 lastmod 增量（首次全量足够，1 小时 revalidate 够用）
- Google Search Console / Bing Webmaster 集成（先通用 SEO）

## Open Questions

1. **chinese-poetry 仓路径**：需在 dev 跑 HEAD 验证 `/全汉诗/乐府诗.json`、`/南北朝/南朝诗.json`、`/南北朝/北朝诗.json` 是否存在。原始记忆中"TCP timeout"状态需要重新测试。
2. **骈文书目列表**：本设计列 5–10 本候选（文心雕龙、昭明文选、骈体文钞、六朝文絜等），需用户最终确认。
3. **prod 域名**：设计用 `https://pinyin.example.com` 占位。实际部署域名需要在 metadata.ts / sitemap.ts / robots.ts 里替换。
4. **sitemap 分片大小**：190K 首诗单 XML ~10MB。如果觉得太大，可改成 `/sitemap/poetry/0.xml`, `/sitemap/poetry/1.xml`（每 50K 一个）；目前不必要，但留口子。

# Redeploy Checklist — 2026-07-09

8 commits on local main to ship (5 wizard+poems + 2 worksheet color + 1 image fallback):

**Wizard + poems (originally 5 commits)**

- `b2dc7163` fix(init-wizard): surface soft-fail (data.failed) + add mark-complete detail
- `877da8a2` fix(init-wizard): bump admin-credentials token TTL 30s → 120s
- `bf72b878` fix(init-wizard): surface step-0 (stash-admin) on /init/execute
- `d84f8c4d` refactor(poems): replace GitHub fetch with data/poems/*.json reader
- `4ba2184a` perf(poems): wrap buildPoems UPSERT in transaction (83s → 2s)

**Worksheet color darkening (user feedback "颜色框太浅了")**

- `f198ea77` fix(worksheet): darken 4-line blue from `#4a90d9` to `#1d4ed8` (balanced w/ red)
- `0f054071` fix(worksheet): darken pen/brush cell borders `#bbb` → `#555` (4-line parity)

**Image optimization fallback (prod `/_next/image` 400)**

- `397d7423` fix(image): disable Next.js Image Optimization (sharp fallback)

## Deploy

```bash
cd Up

npm ci --legacy-peer-deps    # install deps (~3 min)
npm run strokes:build        # ⚠️ DO NOT SKIP — Up/ excludes public/strokes/ (regenerable).
                             #   Without this step every /strokes/{char}.json 404s on prod.
npm run build                # Next.js production build (~3-5 min)
npm start -- -p 4444         # start prod server
```

## Verify in browser

1. Open `https://ziyun.pudafo.com/init` → redirect to `/init/admin`
2. Fill admin form, submit → redirect to `/init/execute`
3. **NEW callout**: green banner "✓ 步骤 0 已完成 — 管理员凭证已缓存 (120s)"
4. Click "开始初始化" → 9 phase cards run in order
5. **poems card**: green ✓ with "新增 1160 行" detail (was: red ✗ "fetch ... 503" pre-d84f8c4d)
6. **mark_complete card**: shows "setup.completed=true,cookie 已同步" detail
7. After all 9 phases → redirect to `/init` locked card → cookie set → home page
8. **NEW worksheet color check**: open `/worksheet/practice?style=pen-english` → print preview
   should show 4 dark blue (`#1d4ed8`) + 2 red (`#d94c4c`/`#c0392b`) rules with much higher
   contrast than pre-f198ea77
9. **NEW Chinese/brush border check**: `/worksheet/new?style=pen-square&paper=A4` → outer
   cell borders should be `#555` (dark gray) not `#bbb` (light gray); inner 田字格/米字格
   guides stay `#bbb` as subtle hints

## What changed for poems (commits d84f8c4d + 4ba2184a)

The wizard's `导入古诗 (data/poems/)` phase used to fetch from `raw.githubusercontent.com/chinese-poetry/chinese-poetry` and 503'd on prod. Now it reads the prebuilt JSONs that already live in `data/poems-manifest.json` + `data/poems/<id>.json` (1160 per-poem files, all with t2s 简体 + pinyin pre-baked). The transaction wrapper brings the insert from ~83s to ~2s on a single commit at the end.

If the previous deploy showed a `failed: fetch ... → 503` for poems, this fixes it without re-running the wizard (the data was already in `data/poems/`; the table gets mirrored on next init from local JSON).

## What changed for worksheet colors (commits f198ea77 + 0f054071)

User feedback "颜色框太浅了" applied to two layers of the printed practice template:

- **f198ea77**: 4-line English template's blue rules were `#4a90d9` (washed out vs the red
  `#c0392b` baseline). Bumped to `#1d4ed8` for visual parity with the red. Updated both
  `app/globals.css` (browser CSS) and `PracticePDF.tsx` (PDF SVG) since 4-line renders via
  CSS in browser and via inline SVG in PDF.

- **0f054071**: pen-square/pen-cross/brush-12/24/28 cell outer borders were `#bbb` (same
  light gray used for inner guides). Distinguishes the printable frame from the subtle
  田字格/米字格 hints. Kept `guideStroke=#bbb` and `charFill=#ddd` so the visual hierarchy
  (border > guide > character) is preserved. Touched `WorksheetCell.tsx` (browser SVG),
  `PracticePDF.tsx` (PDF SVG), and the WorksheetCell test assertion.

No data migration or DB schema change — pure visual.

## What changed for image optimization (commit 397d7423)

Prod `/_next/image?url=/logo.png&w=96&q=75` returned **400 "The requested resource isn't a valid image"** because `sharp-linux-x64` was not installed on the target Linux host. `sharp` is a native binary per-platform — the lockfile from the Windows dev machine shipped `sharp-win32-x64` only, so `npm ci` on Linux couldn't load the right binary. The `/logo.png` references in `app/init/execute`, `app/api/init/init-activate`, and the PDF header all became invisible brand logos.

`next.config.ts` now sets `images.unoptimized: true` — `<Image>` renders the raw src instead of routing through `/_next/image`. Trade-off: no server-side resize / WebP / AVIF, slightly larger bytes on the wire (266KB logo served as-is). Long-term fix:

```bash
cd Up
rm -rf node_modules package-lock.json
npm ci --legacy-peer-deps    # install sharp-linux-x64 properly
npm run build                # MUST rebuild — .next/ embeds the previous config
npm start -- -p 4444
```

Then revert `images.unoptimized` back to `false` in `next.config.ts`. Until then the fallback keeps the brand logo visible regardless of platform.

## If create-admin returns `token_expired`

120s TTL should be enough margin for any reasonable wizard run (poems step is now ~2s with the transaction fix). If it still fails:
- poems phase took >120s on prod — retry the wizard (token is single-use, so re-enter credentials on `/init/admin`)
- wizard tab was open >2 min before clicking start — refresh + re-enter

## Rollback

Revert the 8 commits on local main, re-run `python scripts/copy-to-up.py`, re-sync.